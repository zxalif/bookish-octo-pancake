# Core FastAPI dependencies
# Note: Upgraded to 0.116.0+ to support Starlette 0.40.0+ (security fixes)
# FastAPI 0.115.0 requires starlette<0.38.0, but security fixes need >=0.40.0
fastapi>=0.116.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.6.0

# Database
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10

# Authentication
python-jose[cryptography]==3.4.0  # Fixed: PYSEC-2024-232, PYSEC-2024-233
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # Use 4.0.1 to avoid passlib version detection issues with 4.1.x
python-multipart==0.0.18  # Fixed: GHSA-59g5-xgcq-4qw3

# Payment Gateway (Paddle - PRIMARY)
paddle-python-sdk>=1.12.0  # Official Paddle Python SDK

# HTTP Client (for calling zola-lead API and Paddle API)
httpx==0.27.2

# Caching
redis==5.2.0
hiredis==3.0.0

# Environment & Configuration
python-dotenv==1.0.1

# Email (for notifications)
email-validator==2.2.0

# PDF Generation (for payment receipts)
reportlab==4.2.5

# Utilities
python-dateutil==2.9.0.post0

# Security - HTML Sanitization
bleach==6.1.0

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# Note: Playwright and watchfiles are NOT included here - they're only installed in Dockerfile.e2e-worker
# This keeps the API container lightweight since it doesn't need these E2E testing dependencies

# Monitoring
sentry-sdk[fastapi]==2.8.0  # Fixed: GHSA-g92j-qhmh-64v2

# Rate Limiting
slowapi==0.1.9

# Code Quality (optional)
black==24.10.0
flake8==7.1.1
mypy==1.13.0
